
import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF8875FF);      // Purple used in buttons
  static const Color background = Colors.white;
  static const Color text = Colors.black;
  static const Color textButton = Color(0xFF8875FF);   // Same as primary
  static const Color textFieldFill = Color(0xFFEBEBEB);
  static const Color border = Colors.grey;
}