import 'package:flutter/material.dart';

class AppGradients {
  static const LinearGradient yellowToPink = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [
      Color(0xFFFFDF00), // Yellow
      Color(0xFFFDBCD0), // Pink
    ],
  );
}
